# Control-machine tooling (installed into a virtualenv by scripts/bootstrap-wsl.sh).
# Pinned so every run of the lab uses the same Ansible behaviour.
ansible-core==2.21.4
ansible-lint==26.9.0
yamllint==1.38.0
