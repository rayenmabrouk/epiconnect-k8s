---
- name: Base configuration of every node (packages, accounts, SSH, firewall)
  hosts: k3s_cluster
  become: true
  tags: [base]
  roles:
    - common
    - users
    - ssh_hardening
    - firewall
