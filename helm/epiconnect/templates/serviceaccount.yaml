# Identity with no permissions and no API token: the app never calls the Kubernetes API.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "epiconnect.fullname" . }}
  labels:
    app.kubernetes.io/name: epiconnect
    {{- include "epiconnect.labels" . | nindent 4 }}
automountServiceAccountToken: false
